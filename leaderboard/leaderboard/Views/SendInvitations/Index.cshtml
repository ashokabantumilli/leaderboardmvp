@model IEnumerable<leaderboard.Models.SendInvitation>

@{
    ViewBag.Title = "Index";
}

<h2>Send Invitations</h2>

<p>
    @Html.ActionLink("Send Invite", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailId)
        </th>
<!--
        <th>
            @Html.DisplayNameFor(model => model.GuidId)
        </th>
-->
        <th>
            @Html.DisplayNameFor(model => model.SendDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailId)
        </td>
<!--
        <td>
            @Html.DisplayFor(modelItem => item.GuidId)
        </td>
-->
        <td>
            @Html.DisplayFor(modelItem => item.SendDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
